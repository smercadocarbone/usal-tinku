AGENTE DE CLAUDE (no opencode): tu shell NO conserva el directorio entre comandos (vuelve a un directorio que no es un repo git). Por eso: TODO comando de Bash empieza con `cd {dir} && ...`, y toda ruta que le pases a Read/Edit/Write es ABSOLUTA y está dentro de {dir}. Nunca operes sobre `/Users/santiagomercadocarbone/Documents/Develops/usal/tinku` (el repo principal, con `main` y cambios sin commitear del usuario) ni sobre otro worktree de `tinku-wt/`: hay otros agentes trabajando ahí. La suite de Maven tarda varios minutos: corrésela con un timeout de 600000 ms y guardá la salida en un archivo de /tmp con el nombre de tu worktree (`> /tmp/suite-<slug>.log 2>&1`), después leé la línea `Tests run:` con `rg`. La rama ya tiene `main` mergeado (455 tests): no vuelvas a mergear ni rebasear. PROHIBIDO `git stash` (la pila es compartida entre todos los worktrees y otro agente te puede pisar): si el anexo de CONTINUACIÓN te pide stash para ver el RED de código ya commiteado, en su lugar hacé `git checkout main -- <archivos de src/main>`, corré los tests nuevos y registrá el fallo, y volvé con `git checkout HEAD -- <esos archivos>`; si el código no está commiteado, commitealo primero como WIP.
