# Versiones minimas que soportan Python 3.14 (las fijadas originalmente,
# fastapi 0.115.0 / pydantic 2.9.2 de fines 2024, no tienen wheels para 3.14
# y pydantic-core falla al compilarse desde fuente). T-000-08.
fastapi>=0.141.0
uvicorn[standard]>=0.53.0
pydantic>=2.13.5

# ADR-M2-01 resuelto (pgvector): sentence-transformers para compute los
# embeddings (384 dims, modelo multilingual) + psycopg3 para consultar la
# columna `embedding` de matching.perfiles_tutor_matching (T-M2-04).
sentence-transformers>=6.1.0
psycopg[binary]>=3.2.0
pgvector>=0.5.0

# Dev/test
pytest>=8.0.0
httpx>=0.28.1
ruff>=0.16.8
